export const SMALL_DEPOSIT_ERROR_TEXT = (currency: string, minAmount: string) =>
  `Minimum amount for ${currency.toUpperCase()}: ${minAmount}`;
export const PAIR_INACTIVE_ERROR_TEXT = 'This pair is disabled now';
export const DEFAULT_ERROR_TEXT = 'Something went wrong';
